import React from 'react';import { View, StyleSheet } from 'react-native';import { SkeletonAvatar, SkeletonText, SkeletonImage, SkeletonButton } from './SkeletonBase';import { useTheme } from '../../context/ThemeContext';interface PostSkeletonProps {  showImage?: boolean;  style?: any;}export const PostSkeleton: React.FC<PostSkeletonProps> = ({   showImage = true,   style }) => {  const { colors } = useTheme();  return (    <View style={[styles.container, { backgroundColor: colors.background }, style]}>      {/* Post Header */}      <View style={styles.header}>        <SkeletonAvatar size={44} />        <View style={styles.userInfo}>          <SkeletonText width={120} height={16} style={styles.username} />          <SkeletonText width={80} height={12} />        </View>      </View>      {/* Post Content */}      <View style={styles.content}>        <SkeletonText width="95%" height={16} style={styles.contentLine} />        <SkeletonText width="80%" height={16} style={styles.contentLine} />        {Math.random() > 0.5 && (          <SkeletonText width="60%" height={16} />        )}      </View>      {/* Post Image */}      {showImage && (        <SkeletonImage           width="100%"           height={200}           borderRadius={12}           style={styles.image}         />      )}      {/* Action Buttons */}      <View style={styles.actions}>        <SkeletonButton width={60} height={20} />        <SkeletonButton width={60} height={20} />        <SkeletonButton width={60} height={20} />        <SkeletonButton width={60} height={20} />      </View>    </View>  );};const styles = StyleSheet.create({  container: {    paddingVertical: 12,    paddingHorizontal: 16,    marginBottom: 0,  },  header: {    flexDirection: 'row',    alignItems: 'center',    marginBottom: 12,  },  userInfo: {    flex: 1,    marginLeft: 12,  },  username: {    marginBottom: 4,  },  content: {    marginBottom: 16,  },  contentLine: {    marginBottom: 8,  },  image: {    marginBottom: 16,  },  actions: {    flexDirection: 'row',    justifyContent: 'space-around',    alignItems: 'center',    paddingTop: 16,  },});